def string_to_array(s):
    # your code here
    s = list(s.split(" "))
    return s
    # this uses the list built in function to take a string and convert it to a list
    # we are also using the split method to return the words together and not separate each letter